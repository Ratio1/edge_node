kmonitor 
ratio1>=3.5.41
decentra-vision 
python-telegram-bot[rate-limiter]
protobuf==5.28.3
ngrok
paramiko
pymisp
sqlfluff
neo4j>=5.28,<6
websocket-client>=1.8,<2

# See docs response-excerpt-redaction contract (RM-065): urllib3 2.x caps
# decoded output at the requested read size, which bounds a compressed response
# in the response-fingerprint path. 1.x has no such buffer.
urllib3>=2.0
